# npm 11 起默认不执行依赖的安装脚本，而 esbuild 与 workerd 都需要 postinstall
# 下载平台二进制。缺少二进制时构建会以难以定位的方式失败。
#
# package.json 里的 allowScripts 字段已逐个放行这两个包（比全局
# ignore-scripts=false 更安全：只放行已知需要的，其余仍然拦截）。
# 这里显式记录该行为，避免换机器后行为不一致。
#
# 如果在新环境遇到 "esbuild: Failed to install" 之类的报错，
# 运行 npm install-scripts ls 查看被拦截的脚本，再按需 approve。
save-exact=true
