# 站点基地址 —— 必须是你部署后真实可访问的域名，且不要带末尾斜杠。
#
# 它会被烘焙进 RSS、Sitemap、llms.txt 里的绝对地址。
# 纯静态站没有运行时环境变量，所以只能在构建期注入。
#
# 本地开发：复制本文件为 .env 并改成你的值（.env 已被 gitignore）
# Cloudflare Workers Builds：在 Worker → Settings → Variables and Secrets 里
#   添加同名变量，并在 Build 配置里勾选让它对构建阶段可见。
SITE_ORIGIN=https://your-domain.example
